# 关键开发决策

## 0. 项目总目标（所有迭代的验收标准）

本站不是单纯的资料索引，也不是只展示《银信烽火》的作品页。它要同时完成三件事：

1. 以图像、视频、文稿和互动内容保存、归纳团队此前的成果；
2. 记录团队从调研、整理、研究到创作传播的真实努力与项目历程；
3. 让公众更容易认识侨批，并通过可浏览、可检索、可观看、可参与的方式产生继续了解的兴趣。

因此，公开页面应优先呈现真实可用的图像与影像，并说明地点、成果、来源和关联；结构化字段服务于展陈，不应让网站退化为冷冰冰的表格。每轮修改都要检查“资料保存、历程叙事、公众传播、互动参与”是否同时得到改善。

## 1. 保留而非重写《银信烽火》

原 Beta 被机械迁移到 `src/features/yinxin/YinxinFenghuo.tsx`，仅增加返回总站入口。游戏数据、状态机和视觉样式不参与首轮重构。

## 2. 内容默认不公开

只有 `content/public/` 会被前端加载。`draft` 不打包，`private` 被 Git 忽略；无法确认来源、授权或个人信息边界时，一律不进入公开目录。

## 3. 不制造档案示例

没有侨批原件、完整释读和授权时，档案页宁可显示待接入状态。1923 年菲律宾侨批仅保存为草稿线索。

## 4. 使用结构化 JSON 与 Obsidian 导入脚本

首版用 JSON 保证构建稳定、字段可校验。Obsidian Markdown 经 frontmatter 导入后先进入草稿区，避免维护两套公开内容，同时保留人工核验关口。

## 5. 使用 Hash 路由

现有腾讯云 CloudBase 是纯静态托管。Hash 路由无需添加服务器 rewrite，深层页面刷新不会 404；部署始终复用环境 `yinxin-fenghuo-beta-d7bm0cd14d29`，不另建同名环境。

## 6. 区分历史资料与游戏虚构

科普、实践和研究页面必须列出来源；游戏中的人物、金额、暗语和案件明确标注为互动叙事，不可反向用作历史证据。
